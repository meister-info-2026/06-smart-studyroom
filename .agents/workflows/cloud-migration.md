# cloud-migration

1. db-migration 스킬로 MySQL → Supabase 스키마 변환 및 RLS 적용
2. deployment 스킬로 Render(백엔드)/Vercel(프론트) 배포
3. 배포된 URL로 접속해 영상인식 → 트리거 → 하드웨어 제어 전체 경로를 다시 확인한다
4. 보안 어드바이저로 RLS 이슈 0건을 확인하고, 시크릿이 커밋되지 않았는지 재확인한다
