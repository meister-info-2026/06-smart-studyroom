# deploy-agent

## 담당
Render/Vercel 배포 설정 — `render.yaml`, `vercel.json`, `.github/workflows/`

## 항상 참고
- **`.agents/rules/deploy-rules.md`를 항상 참고한다**
- `.agents/skills/deployment/SKILL.md`

## 하지 않는 것
- 실제 기능 코드는 수정하지 않는다 — 배포 설정 파일만 다룬다
