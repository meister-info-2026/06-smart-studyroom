# 보안 규칙

- `.env` 파일은 절대 커밋하지 않는다. `.gitignore`에 `.env`, `.env.local`을 포함한다 (필수)
- API 키, DB 비밀번호, 디바이스 API 키를 코드에 하드코딩하지 않는다 — 전부 `.env`로 관리한다
- 실제 시크릿 값은 AI 채팅/프롬프트에 절대 직접 입력하지 않는다. 한 번이라도 채팅에
  입력됐다면 노출된 것으로 간주하고 즉시 재발급(로테이션)한다
- `.env.example`에는 키 이름만 두고 실제 값은 절대 넣지 않는다
- 커밋 전 `git diff --cached`로 시크릿이 포함되지 않았는지 직접 확인한다
