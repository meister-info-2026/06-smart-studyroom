# ==============================================================================
# Vision Client Requirements (Windows Python 3.12 권장)
#
# 가상환경 생성 (Python 3.14 DLL 충돌 방지 및 .gitignore 호환):
#   py -3.12 -m venv venv
#   .\venv\Scripts\Activate.ps1
#   $env:PYTHONUTF8=1
#   pip install -r requirements.txt
#
# 얼굴인식 고도화:
#   Windows C++ 빌드 에러를 유발하는 dlib/face_recognition 대신
#   LFW 99.5%+ 인식률의 onnxruntime(경량 ArcFace ONNX) 및 OpenCV DNN(YuNet+SFace) 사용
# ==============================================================================

--extra-index-url https://download.pytorch.org/whl/cpu

# Python 3.10 ~ 3.12 (안정 권장 버전)
torch==2.2.2+cpu; python_version < "3.13"
torchvision==0.17.2+cpu; python_version < "3.13"
numpy<2; python_version < "3.13"
opencv-python==4.9.0.80; python_version < "3.13"

# Python 3.13 이상 (신형 파이썬 환경 호환)
torch==2.9.1+cpu; python_version >= "3.13"
torchvision==0.24.1+cpu; python_version >= "3.13"
opencv-python>=4.10; python_version >= "3.13"

# 핵심 비전 AI 및 랜드마크 분석
ultralytics>=8.0.0
mediapipe; python_version < "3.13"
onnxruntime
scipy

# 웹캠 한글 텍스트 깨짐 방지용
pillow>=10.0.0

# 네트워크 및 환경설정
requests>=2.28.0
python-dotenv>=1.0.0
