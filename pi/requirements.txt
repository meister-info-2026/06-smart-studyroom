# Raspberry Pi 5 Client Dependencies
# ==============================================================================
# [Raspberry Pi 5 Note]
# On Raspberry Pi 5 (RP1), install gpiozero and lgpio via APT:
#   sudo apt update
#   sudo apt install -y python3-gpiozero python3-lgpio
#   python3 -m venv --system-site-packages venv
#   source venv/bin/activate
#   pip install -r requirements.txt
# ==============================================================================

requests>=2.31.0
python-dotenv>=1.0.0
